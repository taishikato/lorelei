# Implementation Plans

Generated by the improve skill on 2026-07-06 (audit of commit `120103a`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo verification baseline (used by every plan):

```
xcodebuild -project Lorelei.xcodeproj -scheme Lorelei -configuration Debug \
  -derivedDataPath DerivedData test \
  -test-timeouts-enabled YES -default-test-execution-time-allowance 60
```

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | CI workflow (build + test every PR) | P1 | S | - | TODO |
| 002 | README refresh (release, gear, links, PostHog disclosure) | P1 | S | - (one optional line after 005) | TODO |
| 003 | JSON decoder: keep integer 0/1 numeric | P1 | S | - | TODO |
| 004 | Remove dead clicky-era code | P2 | S | - | TODO |
| 005 | Track the codex app-server schema snapshot | P2 | S | - | TODO |
| 006 | Router hardening: questions never escalate; desktop turns readOnly | P1 | M | 001 recommended | TODO |
| 007 | Manual update check against GitHub Releases | P2 | M | - | TODO |
| 008 | Guided first-run onboarding | P3 | M | - (line numbers drift after 004) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 001 first: every other plan uses "CI green" as its strongest done-criterion
  once the workflow exists; 006 changes behavior and benefits most.
- 002 step 4 has one OPTIONAL line (linking `docs/appserver-schema/`) that
  only applies if 005 is DONE; everything else in 002 is independent.
- 004 and 008 both touch `CompanionPanelView.swift`/`CompanionManager.swift`
  regions - execute serially (either order), not in parallel worktrees.
- 006's politeness/interrogative lists are English-only by design; the
  Japanese STT follow-up (not planned this round) will need to revisit them.

## Findings considered and rejected / not planned this round

Rejected during vetting (do not re-audit):

- `CodexExecutor.swift`, `CompanionScreenCaptureUtility.swift`,
  `WindowPositionManager.swift` as dead code - all verified LIVE
  (referenced from `CodexAppServerExecutor.swift:103`,
  `AXDesktopActionExecutor.swift:245` + `CompanionManager.swift:1097`, and
  3+ files respectively).
- AX snapshot serializer quadratic behavior - disproven by read; each node
  visited once.
- Audio render-thread allocation churn - disproven; deliberate waveform
  pipeline, locked short appends, O(1024) RMS.
- Test UserDefaults order-dependence - disproven; all 37 suites use unique
  names + `removePersistentDomain`.
- PostHog client key in source - public client-side key, by design (the
  README *disclosure* gap is plan 002).
- Analytics privacy contract violations - none; every capture call site
  verified metadata-only.
- `stringProperty` CFString retain nit in `AudioInputDeviceStore` - not worth
  a plan.

Audited, confirmed real, but NOT selected for planning this round (owner
chose the 8 above; these remain valid findings):

- DEBUG diag log at fixed `/tmp/lorelei-diag.txt` (symlink-followable;
  Release unaffected) - S fix when wanted.
- Unbounded JSON recursion depth / line length from the codex subprocess - S.
- `CodexExecutableLocator` prefers `PATH` over fixed trusted paths - S.
- Streaming render churn (whole-panel re-eval + per-delta scroll animation) - M.
- 4,149-line single test file split - M.
- Lint/format config; CONTRIBUTING/AGENTS.md - S each.
- `CompanionScreenCaptureUtility` has zero test coverage - M.
- `CompanionManager` god object (1,162 lines, ~7 responsibilities) - L;
  requires the test-file split + CI to land first.
- Direction options not selected: Japanese STT spike (strongest
  stated-but-undelivered signal - revisit next round), toolbar text-input
  fallback (draft-text plumbing already exists, idle).
